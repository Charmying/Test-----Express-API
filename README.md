# 測試 CRUD MongoDB 的 API

測試 Express.js 部署到 Render 連接 MongoDB 進行 CRUD 的 API的 API

## 用到的指令

1. npm init -y

2. npm install express mongoose cors body-parser dotenv

3. node server.js

4. npm install bcrypt jsonwebtoken (用於生成和驗證 JWT)

5. npm install socket.io

6. npm install uuid
